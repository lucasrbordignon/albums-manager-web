services:
  frontend:
    container_name: albuns-web
    build: ./frontend
    restart: unless-stopped
    ports:
      - '80:80'
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true
    networks:
      - app-network
